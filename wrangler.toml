name = "silent-sync-backend"
main = "server.ts"
compatibility_date = "2024-04-03"
compatibility_flags = ["nodejs_compat"]

[vars]
# Add these via 'wrangler secret put' for better security in production
# FIREBASE_CONFIG = "..."
# FIREBASE_SERVICE_ACCOUNT = "..."

# Environment variables for local development
[env.development]

[env.production]
